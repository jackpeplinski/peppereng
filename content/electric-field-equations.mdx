---
title: "Electric Field Equations"
metaTitle: "Electric Field Equations"
metaDescription: "This is the meta description"
---
import Dropdown from '../src/components/mdxComponents/dropdown';
import Callout from '../src/components/mdxComponents/callout';
import Divider from '../src/components/mdxComponents/divider';
import { InlineMath, BlockMath } from 'react-katex';
import CoulombLawInline from "../src/components/mathComponents/coulomb's-law-components/coulomb's-law-inline-component"
import ElectricFieldEquations1 from "./assets/electric-field-equations-1.svg";

<img src={ElectricFieldEquations1}/>

<Callout body={<div>
  <InlineMath math="\overrightarrow{E}=\frac{\overrightarrow{F}}{| q_{test} |} \text{ and } \overrightarrow{E}=k\frac{| q_{source} |}{r ^ 2}"/>
  </div>
}/>

- $\overrightarrow{E}$ = electric field generated by $|q_{source}|$
- $|q_{test}|$ = magnitude of $q_{test}$
- $\overrightarrow{F}$ = electrostatic force on $q_{source}$ and $q_{test}$
- Assume $q_{test}$ produces both a negligible electric field and force on $q_{source}$

<CoulombLawInline/>

# FAQ
<Dropdown 
  title = {<span>
    How can we assume <InlineMath math="q_{test}"/> produces both a negligible electric field and force on <InlineMath math="q_{source}"/>?
  </span>}
  body = {<span>
    The strength of the electric is independent of <InlineMath math="q_{test}"/>. <br/>
    <br/>
    <strong>Logically:</strong> A source charge produces electric fields. A test charge only 'tests' the strength of that electric field. That electric field will exist regardless of the presence of a test charge.<br/>
    <br/>
    <strong>Mathematically:</strong>
    <BlockMath math="\text{Coulomb's law: } \overrightarrow{F}=k\frac{|q_{source}||q_{test}|}{r^2} "/>
    <BlockMath math="\text{ Electric field-force equation: } \overrightarrow{E}=\frac{\overrightarrow{F}}{| q_{test} |}"/>
    Both equations reference the same electrostatic force, <InlineMath math="\overrightarrow{F}"/> so let's substitute the expression for Coulomb's Law <InlineMath math="k \frac{q_{test}}{q_{source}}"/>
    into the electric field-force equation.
    <BlockMath math="\overrightarrow{E}=\frac{k \frac{\cancel{q_{test}}q_{source}}{r^{2}}}{\cancel{q_{test}}}"/>
    <BlockMath math="\overrightarrow{E} = k\frac{ |q_{source}| } {r^2}"/>
  </span>}
/>
